// Set to 0 to hide the send endpoints menu.
var enableSend = 1;

// Set to 0 to hide the receive endpoints menu.
var enableReceive = 1;

// 'submit' = send full verdict chain when Submit is clicked
// 'action' = send each verdict individually as it happens
var sendOn = 'submit';

// Set to 0 to include skipped items in sent/submitted verdicts.
var excludeSkipped = 1;
